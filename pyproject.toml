[project]
name = "hyperliquid-bot"
version = "0.0.0"
description = "Phase 0B development foundation for the Hyperliquid trading platform"
requires-python = ">=3.13,<3.14"
dependencies = []

[dependency-groups]
dev = [
    "mypy==2.3.1",
    "pytest==9.1.1",
    "ruff==0.16.4",
]

[tool.uv]
package = false
required-version = "==0.12.5"

[tool.ruff]
required-version = "==0.16.4"
target-version = "py313"
line-length = 100
src = ["src", "tests/python"]
extend-exclude = ["docs"]

[tool.ruff.lint]
select = ["B", "E", "F", "I", "RUF", "UP"]

[tool.mypy]
python_version = "3.13"
strict = true
mypy_path = "src"

[tool.pytest.ini_options]
addopts = ["--strict-config", "--strict-markers"]
pythonpath = ["src"]
testpaths = ["tests/python"]
